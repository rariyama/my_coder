[package]
name = "abc057"
version = "0.1.0"
edition = "2018"

# dependencies added to new project
[dependencies]
 proconio = "*"
 competitive = { git = "https://github.com/tanakh/competitive-rs.git" }

[profile.release]
lto = true
panic = 'abort'
